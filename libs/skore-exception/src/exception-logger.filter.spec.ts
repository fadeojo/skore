import { ExceptionLoggerFilter } from './exception-logger.filter';

describe('ExceptionLoggerFilter', () => {
  it('should be defined', () => {
    expect(new ExceptionLoggerFilter()).toBeDefined();
  });
});
